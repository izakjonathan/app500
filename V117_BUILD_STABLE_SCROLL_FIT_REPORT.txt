V117 BUILD-STABLE SCROLL FIT

Built from V116.

Reason:
- V116 TypeScript compiled correctly.
- Local Next build compiled successfully, but the standard Next worker phase can fail in constrained environments with an EPIPE worker error.
- This version forces the Next build to use a single build worker through NEXT_PRIVATE_BUILD_WORKER=1.

Changes:
- package.json build script:
  NEXT_PRIVATE_BUILD_WORKER=1 next build
- package.json vercel-build script:
  NEXT_PRIVATE_BUILD_WORKER=1 next build

No app UI/scoring/sync logic changed from V116.
