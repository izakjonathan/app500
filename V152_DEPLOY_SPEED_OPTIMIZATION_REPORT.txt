V152 Deploy Speed Optimization

Built from V151.

Changes:
- Added package-lock.json back for faster and more predictable Vercel installs.
- Pinned dependency versions exactly instead of using ^ ranges.
- Kept the stable single-worker production build because default next build produced a worker EPIPE in this environment.
- Added optional build:fast script for testing default Next worker builds later.
- Kept Node 24.x engine.
- Removed node_modules and .next from the ZIP.

Scripts:
- npm run build -> NEXT_PRIVATE_BUILD_WORKER=1 next build
- npm run vercel-build -> NEXT_PRIVATE_BUILD_WORKER=1 next build
- npm run build:fast -> next build

Audit:
- npm install --package-lock-only passed.
- npm ci passed.
- npm run build passed.
- npm run build:fast failed locally with worker EPIPE on Node 22 environment, so stable build script was kept.

Expected deploy improvement:
- package-lock.json should reduce install resolution time and make deploys more cacheable.
- Exact dependency pins should reduce dependency drift and inconsistent installs.

No app/scoring/sync logic changed.
