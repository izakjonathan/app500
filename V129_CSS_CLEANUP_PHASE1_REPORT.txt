V129 CSS Cleanup Phase 1

Built from V128.

Cleaned CSS architecture:
- globals.css: imports only
- globals-base.css: current layout, sizing, spacing and component geometry only
- theme.css: current Blueprint visual system only
- typography.css: current typography controls and active text selectors only

Deleted from CSS:
- old glass/blur/shadow system
- old versioned override blocks
- old unused CSS variables
- legacy selector stacks
- duplicate historical declarations
- stale V*.txt/md/json report files from old builds

Kept current controls:
Typography:
- --font-size-caption
- --font-size-body
- --font-size-title
- --font-size-display
- --font-size-input
- --font-size-score
- --font-weight-label
- --font-weight-body
- --font-weight-title
- --font-weight-score

Spacing:
- --top-section-gap
- --scoreboard-gap
- --last-round-gap
- --input-card-gap
- --penalty-gap
- --bottom-gap

Color:
- --passport-blue
- --passport-bg
- --passport-muted
- --passport-faint

No TSX game/scoring/sync logic intentionally changed.
