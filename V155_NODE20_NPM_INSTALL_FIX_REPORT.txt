V155 Node 20 npm Install Fix

Built from V154.

The Vercel screenshot shows pnpm failing during registry metadata fetch:

ERR_PNPM_META_FETCH_FAIL
ERR_INVALID_THIS

V155 switches away from pnpm and forces the safer baseline:

- Node engine: 20.x
- Install command:
  npm install --legacy-peer-deps --no-audit --no-fund
- Build command:
  npm run vercel-build
- No packageManager field.
- No npm/pnpm/yarn lock files.
- Stable build remains:
  NEXT_PRIVATE_BUILD_WORKER=1 next build

Why:
- Node 24/22 + npm crashed with "Exit handler never called".
- pnpm on Vercel hit registry metadata fetch errors.
- Node 20 LTS + npm is the safest Vercel baseline.

If Vercel still uses an old install command, check:
Project Settings → Build and Deployment

Install Command should be:
npm install --legacy-peer-deps --no-audit --no-fund

Build Command should be:
npm run vercel-build

No app/scoring/sync logic changed.
