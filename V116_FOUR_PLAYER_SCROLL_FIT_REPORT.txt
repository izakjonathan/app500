V116 FOUR PLAYER SCROLL / AUTO-FIT

Built from V115.

Changes:
- Added root app class: players-2 / players-3 / players-4.
- App now scrolls vertically instead of clipping when content exceeds the viewport.
- Four-player mode automatically uses a tighter layout:
  - smaller scoreboard rows
  - smaller total scores
  - tighter last-round card
  - tighter bottom input rows
  - shorter quick buttons
  - two-column -50 penalty buttons
  - shorter Add round button
- Background remains fixed/fullscreen while UI scrolls.
- Scoring/sync/game logic unchanged.
